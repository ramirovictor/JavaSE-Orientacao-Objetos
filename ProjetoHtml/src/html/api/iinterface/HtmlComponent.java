package html.api.iinterface;

public interface HtmlComponent {

    public String toHtml();
    /*
     método que será obrigatóriamente implementado por todas as classes 
     que assinarem o contrato (implementarem a interface).
     */
}
